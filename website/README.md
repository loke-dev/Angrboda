# angrboda.loke.dev

The official Angrboða product and installation site, built with React, vinext,
and Cloudflare Sites.

```sh
npm ci
npm run dev
npm test
```

The page is intentionally static and has no runtime data or authentication. The
theme preview is interactive, responsive, keyboard accessible, and respects
reduced-motion preferences.
